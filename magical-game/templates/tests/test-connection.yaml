apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "magical-game.fullname" . }}-test-connection"
  labels:
    {{- include "magical-game.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "magical-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
